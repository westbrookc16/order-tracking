{
  "name": "orders-project",
  "version": "1.0.0",
  "description": "project for tracking orders",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"cd server&&npm run dev\" \"cd client && npm run dev\""
  },
  "author": "Chris Westbrook",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
